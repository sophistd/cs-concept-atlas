# 设计说明：它是什么，不是什么

> 这份文件解释**为什么这个项目长成现在这样**，以及它有意不做哪些事。
> `README.md` 讲怎么用，这份讲为什么。未定的问题编号 `Q…`，定了就在原处标 **【已定 · 日期】** 并写下理由，**不删旧文**。

---

## §1 判据是「理解」，不是「行动」

一张知识图可以为两种处境服务，判据完全不同：

| | 读者的处境 | 叶子是什么 | 算成功的标志 |
|---|---|---|---|
| **行动** | 我接了个活，从哪下手 | 可点开就能用的工具，带使用成本 | 他**把活干完了** |
| **理解** | 我听到一个词，它到底是什么 | 名词 ＋ 讲清楚的解释 | 他**能跟别人解释清楚** |

**这个项目选「理解」。** 所以它没有一个外链，也不打算有。

判据不同，取舍就完全不同。举个具体的：一张「找工具」的图会收 `shadcn/ui`、`TanStack Query`
这种**当下能用的库**；这张图收的是 `Java`、`Rust`、`哈希表` 这种**能说明某个概念长什么样的典型实例**。
两套选材标准，服务两种人。

---

## §2 三层结构，以及「实体是坐标」这条设计

```
域（24）  →  概念（280）  →  实体（1420）
                              ＋ 跨罩子的关联（610 条，8 种关系）
```

**释义只写在前两层。** 24 个域、280 个概念**全部**有一句话定位，这是硬要求。
而 1420 个实体**绝大多数只有一个名字，这是有意的**，不是没写完。

理由：实体的意义由**它待的位置**给出。`Pinia` 挨着 `Redux`、`Zustand`、`MobX` 挂在
「状态管理」底下 —— 你不用读任何解释，就知道它是干什么的一类东西。
**实体是概念的证据，不是词条。** 给一千多个实体各写一段释义，会把读者的注意力
从「概念之间的关系」拽到「背名词」上，而那正是这张图想避免的。

少数实体确实有长释义（117 条），判据是**它值不值得被单独讲**：
`jQuery` 有，因为它是「库」这个词最标准的样本；`Lodash` 没有，因为它就是又一个库。

---

## §3 和 OSINT Framework 的关系：借了形，没借目的

交互形态（可折叠的树、点开一层层往下）参考了
[OSINT Framework](https://github.com/lockfale/OSINT-Framework) 那一类工具目录，
但**目的不同，所以别拿它的尺子来量这个项目**。

OSINT Framework 是一张**路由表**：叶子是工具链接，带「需注册 / 要本地装」这类使用成本标记，
读者的处境是「我要查某个数据，用哪个工具」。它的
[mission fork](https://github.com/JeanDevenish/OSINT_Framework_Mission_Intelligence)
更进一步，加了「任务 → 阶段 → 工具」的任务轴。

**这个项目按 §1 走的是另一条路**，所以：没有外链、没有工具属性徽标、没有任务轴。
按 OSINT 的判据看，这些全是「缺失」；按「理解」的判据看，它们本来就不该有。

---

## §4 这个项目独有的三样

跟一般的分类树比，多出来的是这三样。它们也是**最该继续投入**的地方：

1. **有语义的边。** 跨罩子的关联不是无类型的「相关」，而是 8 种具体关系，压成三族：
   相似（同一个东西 / 同一类 / 可替代）、共现（固定搭配）、层叠（底下用的是 / 被谁当底座 /
   跑在……之上）。层叠还带方向。**「A 和 B 有关」被拆成了「A 跑在 B 之上」。**
2. **反例节点（14 个）。** 「库 ⟷ 框架：你调用它 vs 它调用你」「抽象 ⟷ 抽象泄漏：
   承诺你不用管 vs 这个承诺永远只兑现一部分」。**它教的是分界线，不是词条** ——
   分不清两个词的时候，人要的是那条线，不是两段各自的定义。
3. **同一个东西的多处落点。** 134 组、297 个节点（占全库 17%）：`JavaScript` 在 4 个概念下
   各有一份，`DOM 树` 3 份。树里它们是不同的点，图谱里合成一个并标 `×N` ——
   **「同一个东西同时属于好几个格子」这件事树画不出来，得靠关联层。**

---

## §5 现在的完成度

不藏着，直接给数：

| | 一句话定位 | 长释义 |
|---|---|---|
| 域 24 | 24 / 24 · **100%** | —— |
| 概念 280 | 280 / 280 · **100%** | 41 / 280 · 14% |

> 概念那一行的 280 是 `t="concept"` 的节点数；界面顶栏显示的 **275** 不含 5 个
> 「最容易混的」容器节点 —— 两个数都对，口径不同。
| 实体 1420 | 0（按 §2，有意不写） | 117 |

**一句话定位是这张图的地基，已经铺满。** 长释义是深度，还在往里填 ——
24 个域现在**每个至少有一条**，但分布很不均：「软件的构成单位」有 9 条，多数域只有 1 条。

---

## §6 协作：结构可以 PR，释义不收

**内容类型决定协作模式。**

工具目录能众包，因为一条工具是 `{名字, 链接, 属性}` —— 客观、可验证、没有风格，
错了一眼能看出来。而这里的释义是**判断和语气**：「React 到底算库还是框架」那一段
是一个说法，不是一个事实。十个人各提一条，声音就散了，而声音正是这个项目的价值。

所以：

- **欢迎 PR**：加节点、加关系、报错报漏、指出分类不对、指出某条释义讲错了
- **释义文字由维护者执笔**：不是不欢迎，是保持一致的说法比多几条更重要
- 大改动先开 issue 聊，别直接 PR

---

## §7 一条合格的概念释义长什么样

三行，合并成一段：

| 行 | 内容 |
|---|---|
| **起源** | **什么困境逼出了它** —— 不是定义。先讲为什么会有这东西，再讲它是什么 |
| **机制** | 它到底怎么运作 |
| **判断** | **分界线在哪 / 最常见的误解是什么** —— 这一行是整条的价值所在，其余是铺垫 |

样例（`覆盖率`）：

> **起源** 写了测试，但不知道漏没漏。于是记录跑测试的时候，哪些行被执行到了。
> **机制** 行覆盖、分支覆盖、路径覆盖，粒度依次变细 —— 分支覆盖会追问「这个 if 的两条路都走过吗」。
> **判断** 覆盖率只能证伪，不能证实 —— 没覆盖到的地方肯定没测，覆盖到了却完全不代表测对了，
> 一个不带任何断言的测试照样能把覆盖率跑满。所以它的正确用法是去看没覆盖的那部分，而不是追一个百分比。

**改稿的正本是 `drafts/*.md`，不是 `data/concepts.json`** —— 释义要被反复改，
改在 340KB 的 JSON 里没法看。

```bash
python3 scripts/merge-drafts.py --check   # 只校验格式
python3 scripts/merge-drafts.py           # 合回 concepts.json
node build.mjs
```

合并脚本会核对下标与概念名对不对得上，对不上直接报错不写。

---

## §8 未定的问题

### Q1 · 长释义的完成度阈值定在哪？【未定】

提议：**每个域至少一半的概念有长释义**。后半句（域和概念 100% 有一句话定位）已达标。
按这个阈值还差 103 条。**但这个数字是拍出来的，等再写一批之后回头校准。**

### Q2 · 21 组「组合」要不要升成首屏入口？【未定】

现在藏在「组合」按钮后面。但它其实是这个项目里唯一**不要求读者先知道自己要找什么**的入口 ——
在「理解」这个判据下，读者最常见的处境恰恰是「我对这一坨完全没概念」。

另：那 21 组是聚类算出来的，有三簇带杂质要剪 ——「Python 与模型训练」混进了 MVC、
「关系型数据库」混进了 Terraform、「版本控制」混进了「树」。

### Q3 · 写给谁？【未定】

现在的语气是对着**一个懂点技术、但被术语绕晕的中文读者**说话
（「你调用它 vs 它调用你」「换掉伤筋动骨 vs 换掉等于重做」）。这个声音是好的，但从没被明确写下来 ——
不写下来，后续补的条目会不知不觉滑成维基风格。

### Q4 · 要不要引入「任务 / 场景」轴？【倾向不要】

即 OSINT 那个 fork 的做法。**任务轴服务于「行动」，跟 §1 的判据冲突**，
硬加会让这个项目变成既不是路由表也不是图谱。已有的 21 组「组合」已经是一个弱化的场景轴。
