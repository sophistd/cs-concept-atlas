# 参与 CS/SE 导航地图

欢迎补充资源、修正分类、改写解释、补充关系与纠错。目标是帮助刚通过 AI 或 Coding Agent 接触编程的读者，认识技术部件并找到继续探索的入口。贡献通过普通 PR 由维护者审核；常规内容修正可以直接提交。

先读 [当前定位](intent.md)，再用 [内容贡献模板](design/contribution-template.md) 准备改动。涉及读者定位、入口结构或对象模型的变化，先开 issue 说明原因；不需要为每条解释另走审批流程。

## 先选对正本

| 要改什么 | 在哪里改 |
|---|---|
| 新导航样板的概念、资源、关系、入口和来源 | [design/content.json](design/content.json)。来源支撑范围同步写在 [design/sources.md](design/sources.md)。 |
| 样板交互、布局或阅读验收 | `design/` 对应源码及 [design/review.md](design/review.md)。先读 [模块地图](design/CLAUDE.md)。 |
| 全景节点的解释与相关资料 | `data/entries/` 对应领域 JSON。先读 [字段与条目规则](data/entries/CLAUDE.md)。 |
| 既有全景原型的节点或关系 | `data/concepts.json`。它仍使用数组下标，尚未迁移为样板的稳定 ID。 |
| 历史长释义 | `drafts/*.md`，再经合并脚本写回旧数据；不覆盖 entries 的当前正文。 |

`design/index.html` 与根目录 `index.html` 都是生成物，修改源码后构建，不直接编辑 HTML。新样板与旧原型的数据接口不同，不把样板对象直接塞进旧数组。

## 全景条目与来源

在对应领域 JSON 的 `entries` 中修改或补充记录：

```json
{
  "node": 946,
  "name": "SQLite",
  "summary": "在应用进程中运行的嵌入式 SQL 数据库引擎。",
  "explanation": "应用通过库读写本地数据库文件，不需要独立数据库服务。一个数据库文件同一时刻只有一个写事务，多个写入者需要协调。",
  "sourceIds": ["sqlite-intro"]
}
```

`node` 与 `name` 必须匹配分类库；条目不能跨领域放置。来源记录放在该文件的 `sources` 中，包含局部唯一的 `id`、`title`、完整 `url` 与说明支撑范围的 `scope`。只有实际打开核对后才填写 `checkedAt`（YYYY-MM-DD），不要按提交日期批量填充。请优先修改现有来源，不重复复制同一链接。

构建仅根据原库显式“同一个东西”关系复用基础正文，代表是最小节点编号。修改代表条目会影响所有落点；修改其他落点只补充当前领域的用法。非代表条目的正文与来源会保留在详情中，不会被静默丢弃。不要用这种关系连接仅同类或可能替代的对象。

全景修改后执行：

```bash
node build.mjs
node build.mjs --check
node scripts/content-checks.mjs
```

在首页搜索改过的节点，检查最底层说明、资料入口、上层概念和其他落点。字段齐全不是内容审核结论。

## 内容怎么写

- **概念**先讲为什么会需要它，再讲机制与边界；例子应使读者能想象软件在做什么。
- **资源**写清类型、用途、运行环境、使用条件与限制，并提供官方入口。安装服务、注册账号、付费托管是不同条件，应分别说明实际需要哪一个。
- **关系**写出两端对象、含义与条件。同属一类不表示可以直接替换；比较需要明确比较的需求和边界。
- **来源**优先采用官方介绍与技术手册，记录所核对的页面和日期。改动用途、限制或关系时重读相关资料；只改标点不等于完成一次重新核实。
- **复用**用 ID 引用现有对象。同一资源的正文、名称、链接只维护一次，三个入口只保存路径和引用。

本轮样板支持两类关系：`implements` 连接资源与概念，`comparable` 连接两个可围绕特定需求比较的资源。需要其他语义时先说明真实内容需求，避免把它勉强标成“相关”。

完整范例已经保存在 [design/content.json](design/content.json)：资源 `sqlite`、`postgresql`，概念 `data-storage` 等，以及关系 `sqlite-postgresql-comparison`。模板解释字段和提交流程，不复制第二份资源正文。

## 生成与检查

修改样板后，在仓库根目录运行：

```bash
node design/build.mjs          # 校验并更新 design/index.html
node design/build.mjs --check  # 只检查内容、语法及产物同步
node design/checks.mjs         # 内存负例，验证校验器能拒绝坏数据
```

通过本地 HTTP 或双击生成物查看样板，分别从三个入口走到改过的资源。确认对象说明一致、官方链接指向相应项目、使用条件在页面上可读。

机械检查只能发现字段、重复 ID、引用、链接格式及构建同步问题；链接格式合法不证明链接仍可访问，检查通过也不证明解释准确或读者已经理解。维护者继续核对来源和表达。

只改全景时，使用 [README 的检查命令](README.md#检查与维护)；不要为了提交样板而重新生成旧原型。

## PR 写清楚什么

用 [模板中的提交说明](design/contribution-template.md#提交说明) 说明改变了什么、帮助读者回答什么问题、依据哪些来源，以及验证结果。不必贴出完整资源正文；指向实际修改的 ID 即可。

维护者主要看：收录是否有必要、是否已有同一对象、概念归属与关系是否准确、来源是否支持表述、条件有没有遗漏、中文是否适合目标读者。修改结构或接口时同步检查文件契约和模块地图，保持源码与文档一致。

内容沿用仓库的 CC BY-SA 4.0，代码沿用 MIT，详见 [内容许可](LICENSE-CONTENT) 与 [代码许可](LICENSE)。引用资料应转述与链接，不复制未经许可的长段正文。

[PROTOCOL]: 变更时更新此头部，然后检查 CLAUDE.md
